# Database
DB_HOST=127.0.0.1
DB_NAME=mystic
DB_USER=root
DB_PASS=secret

# App environment (local|staging|production)
MYSTIC_ENV=local

# Allow scheduler to activate promotions in non-production
DROP_SCHEDULER_ALLOW_ACTIVATE=false

# Local PHP executable path (Windows)
PHP_PATH=C:\xampp\php\php.exe

# Optional webhook for alerts
WEBHOOK_URL=
WEBHOOK_AUTH_HEADER=
WEBHOOK_AUTH_VALUE=
